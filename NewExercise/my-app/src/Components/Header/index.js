import styles from "@/Components/Header/header.module.css";

const Header = () => {
    return (
        <header>
            <h1 className={styles.title}>Next js app with app router</h1>
        </header>
    )
}

export default Header;